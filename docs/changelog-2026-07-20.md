# Changelog — 2026-07-20: Claude Code handoff (implementation phase begins)

- **CLAUDE-CODE-HANDOFF.md (NEW)**: first-session brief for Claude Code —
  Milestone 1 (prove the content pipeline: Astro + zod content collections
  render hexagons and skills at localhost, no UI/DB/auth), what to read, repo
  layout rule (app in its own folder), hard rules from the decision log, and
  design-fidelity expectations for later milestones.
- **CLAUDE.md**: graduated from "planning phase, no app code" to
  "IMPLEMENTATION"; added the stack section (Astro/Cloudflare/zod, template =
  steamhead-site-rebuild, app in its own top-level folder).
- **decisions.md**: D25 — Claude Code handoff; ONE repo holding content + app
  (amends D10: sibling = beside the marketing site, not app-beside-content);
  rebuild prototypes as components; follow the template with room to propose
  changes; defer all private/runtime infrastructure past the first milestones.

Setup steps for getting into Claude Code live in
claude-code-setup-steps.md (delivered alongside this push, not committed unless
James wants it in guides/).
